<div class="container">
  <div class="page-header">
    <% if notice %>
    <div class= "alert alert-success"><%= notice %></div>
    <% end %>

    <div class="pull-right">
      <%= link_to 'Edit', edit_location_event_path(@location, @event), class: "btn btn-info" %>
      <%= link_to 'Delete', location_event_path(@location, @event), class: "btn btn-danger", method: :delete, data: { confirm: 'Are you sure?' } %></td></div>
      <h1><%= @event.description %></h1>
    </div>

    <dl class="dl-horizontal">
      <dt>Date: </dt>
      <dd><%= @event.date %></dd><br>
      <dt>Requires : </dt>
      <dd><%= @event.requires_id %></dd><br>
    </dl>
